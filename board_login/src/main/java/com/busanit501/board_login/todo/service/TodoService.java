package com.busanit501.board_login.todo.service;

import com.busanit501.board_login.todo.dto.TodoDTO;

public enum TodoService {
     INSTANCE;

     public void register(TodoDTO todoDTO) {
         
     }
}
